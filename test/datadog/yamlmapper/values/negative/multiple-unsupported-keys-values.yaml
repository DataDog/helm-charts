# Test values with multiple unsupported Helm keys that should cause mapper to error
# This tests that the mapper correctly rejects multiple unknown/unmapped keys
datadog:
  apiKey: "00000000000000000000000000000000"
  appKey: "0000000000000000000000000000000000000000"
  kubelet:
    tlsVerify: false

# Multiple unsupported keys - all should be flagged as errors
fakeTopLevelKey:
  someValue: true
  anotherValue: "test"

anotherFakeKey: "this-should-error-too"

unmappedFeature:
  enabled: true
  config:
    setting: "value"

