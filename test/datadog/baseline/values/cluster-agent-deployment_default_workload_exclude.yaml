clusterAgent:
  enabled: true
  celWorkloadExclude:
    - products: ["global"]
      rules:
        containers:
          - container.name == "redis"
    - products: ["logs", "metrics"]
      rules:
        kube_service:
          - kube_service.name == "nginx"
