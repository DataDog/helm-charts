apiVersion: v1
kind: Service
metadata:
  name: {{ include "datadog-local.fullname" . }}
  labels:
    {{- include "datadog-local.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "datadog-local.selectorLabels" . | nindent 4 }}
