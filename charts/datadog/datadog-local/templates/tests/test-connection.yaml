apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "datadog-local.fullname" . }}-test-connection"
  labels:
    {{- include "datadog-local.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "datadog-local.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
