{{ if and .Values.datadog.operator.enabled (.Capabilities.APIVersions.Has "datadoghq.com/v2alpha1/DatadogAgent") }}
apiVersion: datadoghq.com/v2alpha1
kind: DatadogAgent
metadata:
  name: {{ template "datadog.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "datadog.labels" . | indent 4 }}
    app.kubernetes.io/component: agent
spec:
  global:
    clusterName: {{ .Values.datadog.clusterName }}
    kubelet:
      tlsVerify: {{ .Values.datadog.kubelet.tlsVerify }}
    credentials:
      apiSecret:
        secretName: {{ template "datadog.apiSecretName" . }}
        keyName: api-key
      appSecret:
        secretName: {{ template "datadog.appKeySecretName" . }}
        keyName: app-key
  {{- end }}