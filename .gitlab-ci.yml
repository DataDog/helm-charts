e2e:
#  rules:
#    - if: $CI_COMMIT_BRANCH == "main"
  image: 486234852809.dkr.ecr.us-east-1.amazonaws.com/ci/test-infra-definitions/runner:a58cc31c
  tags: ["arch:amd64"]
  before_script:
    - apt-get update
    - apt-get install -y jq
    - export TEST_PARAM=$(aws ssm get-parameter --region us-east-1 --name ci.helm-charts.test-param --with-decryption --query "Parameter.Value" --out text)
    # Setup AWS Credentials
    - mkdir -p ~/.aws
    - set +x
    - aws ssm get-parameter --region us-east-1 --name ci.helm-charts.e2e-profile --with-decryption --query "Parameter.Value" --out text >> ~/.aws/config
    - set -x
    - export AWS_PROFILE=helm-charts-e2e-ci
#    - pulumi login "s3://dd-pulumi-state?region=us-east-1&awssdk=v2&profile=$AWS_PROFILE"
  script:
    - echo $TEST_PARAM
#    - make test-e2e
