---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: datadoginstrumentations.datadoghq.com
spec:
  group: datadoghq.com
  names:
    kind: DatadogInstrumentation
    listKind: DatadogInstrumentationList
    plural: datadoginstrumentations
    shortNames:
      - ddi
    singular: datadoginstrumentation
  scope: Namespaced
  versions:
    - additionalPrinterColumns:
        - jsonPath: .spec.targetRef.kind
          name: Target Kind
          type: string
        - jsonPath: .spec.targetRef.name
          name: Target Name
          type: string
        - jsonPath: .status.conditions[?(@.type=='ChecksReady')].status
          name: Checks Ready
          type: string
        - jsonPath: .metadata.creationTimestamp
          name: Age
          type: date
      name: v1alpha1
      schema:
        openAPIV3Schema:
          description: DatadogInstrumentation is the Schema for the datadoginstrumentations API.
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: DatadogInstrumentationSpec defines the desired state of DatadogInstrumentation.
              properties:
                config:
                  description: Config defines the Datadog instrumentation configuration to apply to the target workload.
                  properties:
                    apm:
                      description: APM configures the APM product through Single Step Instrumentation for the target workload.
                      properties:
                        ddTraceConfigs:
                          description: |-
                            TracerConfigs is a list of configuration options to use for the installed SDKs. These options will be added
                            as environment variables to the workload in addition to the configured SDKs.
                          items:
                            description: EnvVar represents an environment variable present in a Container.
                            properties:
                              name:
                                description: |-
                                  Name of the environment variable.
                                  May consist of any printable ASCII characters except '='.
                                type: string
                              value:
                                description: |-
                                  Variable references $(VAR_NAME) are expanded
                                  using the previously defined environment variables in the container and
                                  any service environment variables. If a variable cannot be resolved,
                                  the reference in the input string will be unchanged. Double $$ are reduced
                                  to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                  "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                  Escaped references will never be expanded, regardless of whether the variable
                                  exists or not.
                                  Defaults to "".
                                type: string
                              valueFrom:
                                description: Source for the environment variable's value. Cannot be used if value is not empty.
                                properties:
                                  configMapKeyRef:
                                    description: Selects a key of a ConfigMap.
                                    properties:
                                      key:
                                        description: |-
                                          The key to select from the ConfigMap's Data field.
                                          Keys in the BinaryData field are not currently propagated to container env vars.
                                        type: string
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: Specify whether the ConfigMap or its key must be defined
                                        type: boolean
                                    required:
                                      - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  fieldRef:
                                    description: |-
                                      Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                      spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                    properties:
                                      apiVersion:
                                        description: Version of the schema the FieldPath is written in terms of, defaults to "v1".
                                        type: string
                                      fieldPath:
                                        description: Path of the field to select in the specified API version.
                                        type: string
                                    required:
                                      - fieldPath
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  fileKeyRef:
                                    description: |-
                                      FileKeyRef selects a key of the env file.
                                      Requires the EnvFiles feature gate to be enabled.
                                    properties:
                                      key:
                                        description: |-
                                          The key within the env file. An invalid key will prevent the pod from starting.
                                          The keys defined within a source may consist of any printable ASCII characters except '='.
                                          During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                        type: string
                                      optional:
                                        default: false
                                        description: |-
                                          Specify whether the file or its key must be defined. If the file or key
                                          does not exist, then the env var is not published.
                                          If optional is set to true and the specified key does not exist,
                                          the environment variable will not be set in the Pod's containers.

                                          If optional is set to false and the specified key does not exist,
                                          an error will be returned during Pod creation.
                                        type: boolean
                                      path:
                                        description: |-
                                          The path within the volume from which to select the file.
                                          Must be relative and may not contain the '..' path or start with '..'.
                                        type: string
                                      volumeName:
                                        description: The name of the volume mount containing the env file.
                                        type: string
                                    required:
                                      - key
                                      - path
                                      - volumeName
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  resourceFieldRef:
                                    description: |-
                                      Selects a resource of the container: only resources limits and requests
                                      (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                    properties:
                                      containerName:
                                        description: 'Container name: required for volumes, optional for env vars'
                                        type: string
                                      divisor:
                                        anyOf:
                                          - type: integer
                                          - type: string
                                        description: Specifies the output format of the exposed resources, defaults to "1"
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      resource:
                                        description: 'Required: resource to select'
                                        type: string
                                    required:
                                      - resource
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  secretKeyRef:
                                    description: Selects a key of a secret in the pod's namespace
                                    properties:
                                      key:
                                        description: The key of the secret to select from.  Must be a valid secret key.
                                        type: string
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: Specify whether the Secret or its key must be defined
                                        type: boolean
                                    required:
                                      - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                type: object
                            required:
                              - name
                            type: object
                          type: array
                          x-kubernetes-list-map-keys:
                            - name
                          x-kubernetes-list-type: map
                        ddTraceVersions:
                          additionalProperties:
                            type: string
                          description: |-
                            TracerVersions is a map of SDK versions to install for target workload. The key is the language name and the
                            value is the version to use. If omitted, all default supported SDKs will be added to the application runtime.
                          type: object
                        enabled:
                          default: true
                          description: |-
                            Enabled turns on APM via Single Step Instrumentation to automatically install the Datadog SDKs for supported
                            languages with no additional configuration required.
                          type: boolean
                      type: object
                    checks:
                      description: Checks configures Datadog Agent Autodiscovery checks for the target workload.
                      items:
                        description: DatadogInstrumentationCheckConfig defines an Autodiscovery check configuration.
                        properties:
                          containerName:
                            description: ContainerName identifies the container name this check applies to.
                            type: string
                          initConfig:
                            description: InitConfig is the integration-specific Autodiscovery init_config payload.
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                          instances:
                            description: Instances contains integration-specific Autodiscovery instances payloads.
                            items:
                              type: object
                              x-kubernetes-preserve-unknown-fields: true
                            type: array
                            x-kubernetes-list-type: atomic
                          integration:
                            description: Integration is the Datadog integration name, for example redisdb.
                            type: string
                        required:
                          - integration
                        type: object
                      type: array
                      x-kubernetes-list-type: atomic
                    logs:
                      description: Logs configures Datadog Agent log collection for the target workload.
                      items:
                        description: DatadogInstrumentationLogConfig defines Agent log collection configuration fields.
                        properties:
                          allowed_ips:
                            description: AllowedIPs lists IP addresses or CIDR ranges allowed to send TCP or UDP logs.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: set
                          attribute_parsing:
                            description: AttributeParsing controls whether the full syslog parser is enabled for this source.
                            type: boolean
                          auto_multi_line:
                            description: AutoMultiLine defines detailed automatic multi-line detection options for this source.
                            properties:
                              enable_datetime_detection:
                                description: EnableDatetimeDetection controls detection of multi-line logs based on leading timestamps.
                                type: boolean
                              enable_json_aggregation:
                                description: EnableJSONAggregation controls aggregation of multi-line JSON logs.
                                type: boolean
                              enable_json_detection:
                                description: EnableJSONDetection controls detection of JSON logs so they are not aggregated as unstructured multi-line logs.
                                type: boolean
                              pattern_table_max_size:
                                description: PatternTableMaxSize sets the maximum number of patterns used by automatic multi-line detection.
                                format: int32
                                minimum: 1
                                type: integer
                              stack_trace_parsers:
                                description: StackTraceParsers overrides the enabled stack trace parsers. An empty list disables stack trace aggregation.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                              tag_aggregated_json:
                                description: TagAggregatedJSON controls whether aggregated JSON logs are tagged.
                                type: boolean
                              tokenizer_max_input_bytes:
                                description: TokenizerMaxInputBytes sets the maximum number of bytes tokenized from each log line.
                                format: int32
                                type: integer
                            type: object
                          auto_multi_line_detection:
                            description: AutoMultiLineDetection enables automatic multi-line detection for this source.
                            type: boolean
                          auto_multi_line_detection_custom_samples:
                            description: AutoMultiLineDetectionCustomSamples defines samples and regular expressions used by automatic multi-line detection.
                            items:
                              description: DatadogInstrumentationLogAutoMultiLineSample defines a sample or regular expression used by automatic multi-line detection.
                              properties:
                                label:
                                  description: Label is applied when the sample or regular expression matches.
                                  type: string
                                regex:
                                  description: Regex is a regular expression used to identify the start of a multi-line log.
                                  type: string
                                sample:
                                  description: Sample is a representative raw log message used to identify the start of a multi-line log.
                                  type: string
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          bind_host:
                            description: BindHost is the host or IP address on which a TCP or UDP log source listens.
                            type: string
                          channel_path:
                            description: ChannelPath is the Windows event channel path when type is windows_event.
                            type: string
                          containerName:
                            description: ContainerName identifies the container name this log configuration applies to.
                            type: string
                          denied_ips:
                            description: DeniedIPs lists IP addresses or CIDR ranges denied from sending TCP or UDP logs.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: set
                          encoding:
                            description: |-
                              Encoding sets the file encoding when type is file.
                              Common values include utf-16-le, utf-16-be, and shift-jis.
                            type: string
                          exclude_paths:
                            description: ExcludePaths lists matching files to exclude when type is file and path contains a wildcard.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: set
                          exclude_units:
                            description: ExcludeUnits lists journald units to exclude when type is journald.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: set
                          fingerprint_config:
                            description: FingerprintConfig defines per-source file fingerprinting options.
                            properties:
                              count:
                                description: Count is the number of lines or bytes used to compute the fingerprint.
                                format: int32
                                minimum: 1
                                type: integer
                              count_to_skip:
                                description: CountToSkip is the number of lines or bytes skipped before computing the fingerprint.
                                format: int32
                                minimum: 0
                                type: integer
                              fingerprint_strategy:
                                description: FingerprintStrategy sets the fingerprinting strategy.
                                enum:
                                  - line_checksum
                                  - byte_checksum
                                  - disabled
                                type: string
                              max_bytes:
                                description: MaxBytes limits the number of bytes used for line-based fingerprinting.
                                format: int32
                                minimum: 1
                                type: integer
                            required:
                              - fingerprint_strategy
                            type: object
                          format:
                            description: Format sets the parsing format for this log source. The supported structured format is syslog.
                            type: string
                          idle_timeout:
                            description: IdleTimeout is the duration before an idle TCP connection is closed.
                            type: string
                          include_units:
                            description: IncludeUnits lists journald units to include when type is journald.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: set
                          log_processing_rules:
                            description: LogProcessingRules contains Agent log processing rules for this log source.
                            items:
                              type: object
                              x-kubernetes-preserve-unknown-fields: true
                            type: array
                            x-kubernetes-list-type: atomic
                          max_connections:
                            description: MaxConnections is the maximum number of concurrent TCP connections.
                            format: int32
                            type: integer
                          max_message_size_bytes:
                            description: MaxMessageSizeBytes overrides the global maximum message size for this source.
                            format: int32
                            minimum: 1
                            type: integer
                          path:
                            description: Path is the file path for gathering logs when type is file or journald.
                            type: string
                          port:
                            description: Port is the port for listening to logs when type is tcp or udp.
                            format: int32
                            type: integer
                          service:
                            description: Service sets the log service name.
                            type: string
                          source:
                            description: Source sets the log source name.
                            type: string
                          sourcecategory:
                            description: SourceCategory sets the source category attribute.
                            type: string
                          start_position:
                            description: |-
                              StartPosition sets where the Agent starts reading for file and journald tailers.
                              Common values include beginning, end, forceBeginning, and forceEnd.
                            type: string
                          tags:
                            description: Tags sets additional tags on collected logs.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: set
                          type:
                            description: Type is the type of log input source. Common values include tcp, udp, file, windows_event, docker, and journald.
                            type: string
                        required:
                          - containerName
                        type: object
                      type: array
                      x-kubernetes-list-type: atomic
                  type: object
                targetRef:
                  description: TargetRef is the reference to the workload resource to instrument.
                  properties:
                    apiVersion:
                      description: apiVersion is the API version of the referent
                      type: string
                    kind:
                      description: 'kind is the kind of the referent; More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
                      type: string
                    name:
                      description: 'name is the name of the referent; More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                      type: string
                  required:
                    - kind
                    - name
                  type: object
              required:
                - config
                - targetRef
              type: object
            status:
              description: DatadogInstrumentationStatus defines the observed state of DatadogInstrumentation.
              properties:
                conditions:
                  description: Conditions represent the latest available observations of the instrumentation handlers.
                  items:
                    description: Condition contains details for one aspect of the current state of this API Resource.
                    properties:
                      lastTransitionTime:
                        description: |-
                          lastTransitionTime is the last time the condition transitioned from one status to another.
                          This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          message is a human readable message indicating details about the transition.
                          This may be an empty string.
                        maxLength: 32768
                        type: string
                      observedGeneration:
                        description: |-
                          observedGeneration represents the .metadata.generation that the condition was set based upon.
                          For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                          with respect to the current state of the instance.
                        format: int64
                        minimum: 0
                        type: integer
                      reason:
                        description: |-
                          reason contains a programmatic identifier indicating the reason for the condition's last transition.
                          Producers of specific condition types may define expected values and meanings for this field,
                          and whether the values are considered a guaranteed API.
                          The value should be a CamelCase string.
                          This field may not be empty.
                        maxLength: 1024
                        minLength: 1
                        pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                        type: string
                      status:
                        description: status of the condition, one of True, False, Unknown.
                        enum:
                          - "True"
                          - "False"
                          - Unknown
                        type: string
                      type:
                        description: type of condition in CamelCase or in foo.example.com/CamelCase.
                        maxLength: 316
                        pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                        type: string
                    required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                    type: object
                  type: array
                  x-kubernetes-list-map-keys:
                    - type
                  x-kubernetes-list-type: map
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
